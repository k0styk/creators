const {searchCases} = require('./searchCases');

module.exports = {
    getCase: async ({params, knex}) => {
        const userId = 1;
        // const cards = await knex("cases")
        //        fisrt
        //            .join
        // return cards;
    }
};
